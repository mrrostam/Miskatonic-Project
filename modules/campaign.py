import constant
import os
import json
import player
import chaosbag
import mycard
import scenarios

class Campaign():
    def __init__(self, name = "Night of the Zealot", players_list = None, difficulty_level = 'easy', chaos_bag=None):
        self.players = players_list
        self.difficulty_level = difficulty_level
        self.chaos_bag = chaos_bag

if __name__ == "__main__":
    campaign_name = "Night of the Zealot"
    Player("player", "Roland Banks", 1, scenarios)
    player = [Pl]
    my_campaign = Campaign